import React from "react";

function ConfigurationPage(props) {
  return (
    // <> is same as <React.Fragment>
    <>
      <p>Display the configuration for {props.logId} </p>
    </>
  );
}

export default ConfigurationPage;
